/**
 * Knot v2 — Chapbook 2 Special Passages
 *
 * Declarative definitions for Chapbook's named and tag-based
 * special passages. Chapbook 2 has no special passages beyond
 * the basic Twee 3 spec, so this is an empty array.
 *
 * MUST NOT import from: core/, handlers/
 */

import type { SpecialPassageDef } from '../_types';

export const SPECIAL_PASSAGES: readonly SpecialPassageDef[] = [];
